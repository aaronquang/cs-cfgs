// Spawns a bot on your position and puts you on top
alias +bot_boost "getpos | alias anglefix setang; getpos | alias boost; boost; setang 90 0 0; bot_place"
alias -bot_boost "getpos | alias boost; boost; anglefix"
